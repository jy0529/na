[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "na"
version = "0.1.0"
description = "Nick Agent"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openai>=2.15.0",
    "pydantic-settings>=2.12.0",
]

[tool.hatch.build.targets.wheel]
packages = ["llm", "patterns"]
